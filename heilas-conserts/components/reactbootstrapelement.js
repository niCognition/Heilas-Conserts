import React from 'react';
import { Button } from 'react-bootstrap';

const ReactButtons = () => {

    return (
        <>
        <Button variant="primary">Primary theme button</Button>
        <Button variant="secondary">Secondary theme button</Button>
        <Button variant="success">Success theme button</Button>
        <Button variant="danger">Danger theme button</Button>
        <Button variant="warning">Warning theme button</Button>
        <Button variant="info">Info theme button</Button>
        <Button variant="dark">Dark theme button</Button>
        </>
    );
}

export default ReactButtons;